# Cookies Club v1.1 — visuel & UX : plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Intégrer l'identité Cookies Club (typos, logo, icônes de notes, marqueurs), le toggle sombre/clair, les correctifs UX de la fiche (gestes, icônes, modal itinéraire, boutons stables) et le socle SEO bilingue — en une branche et une PR.

**Architecture:** Le thème devient un attribut `data-theme` sur `<html>` piloté par `src/lib/theme.ts` (localStorage > système, script inline anti-FOUC) ; tout ce qui lisait `prefers-color-scheme` (CSS, `currentTheme()`) lit désormais cet attribut, ce qui synchronise les deux cartes et l'admin sans les coupler. Les SVG de marque sont optimisés une fois et montés en sprite global (`<use>` partagé par notes ET marqueurs). Les constantes de nom/domaine vivent dans `src/lib/site.ts`, consommées par metadata, manifest, sitemap et JSON-LD.

**Tech Stack:** Next.js 16.3.0 (App Router), React 19.2.8, Tailwind v4 (`@theme inline`), maplibre-gl 6.2 (ESM-only, `import * as`), Leaflet 1.9, Vitest 4 + @testing-library/react (jsdom), next/font, next/og (satori), svgo.

## Global Constraints

- Spec de référence : `docs/superpowers/specs/2026-08-08-ajustements-visuels-v1.1-design.md`.
- Nom textuel partout : **« Cookies Club — Montréal »** ; marque visuelle (logo) : **« Cookies Club »** ; plus aucune occurrence visible de « Cookies MTL ».
- Domaine de prod : **https://cookies.zucchinistudio.com** (jamais l'URL vercel.app dans les metadata).
- **Jamais d'emoji ni de caractère décoratif (✕ ◎ ↗ ✓ 🍪) dans l'UI** : icônes SVG en trait (`stroke="currentColor"`, `strokeWidth={2}`, `fill="none"`, `aria-hidden`), 16 px par défaut.
- Tout libellé utilisateur passe par `src/lib/i18n.ts`, ajouté en **fr ET en** (l'admin peut rester FR pur, il est géré en français).
- La police `src/fonts/gill-sans-ultra-bold.otf` existe déjà (fournie par Léo). Ne jamais la référencer depuis `public/`.
- Requêtes SEO cibles : « cookie map », « cookie montréal », « cookies mtl », « meilleurs cookies montréal », « best cookies montreal ».
- Commits en français, préfixes `feat:`/`fix:`/`test:`/`docs:` (convention du repo). Branche : `feature/v1.1-visuel-ux` (existe déjà). Ne PAS merger : Léo QA en local d'abord.
- Vérification par tâche : `npm test` (vitest run) doit rester vert ; `npx eslint src` sans nouvelle erreur. Matchers vitest de base uniquement (pas de jest-dom).
- maplibre-gl 6.x : ESM-only sans export default ; `preserveDrawingBuffer` passe dans `canvasContextAttributes` (pièges déjà documentés dans le code).
- Ne pas toucher : pipeline de tuiles (`scripts/`), `public/tiles/`, logique de bascule WebGL→raster.

---

### Task 1 : Typographies — Gill Sans Ultra Bold + Comfortaa

**Files:**
- Modify: `src/app/layout.tsx` (imports de fonts, lignes 2, 6-24, 40)
- Modify: `src/app/globals.css` (`@theme inline` lignes 91-97, `body` ligne 102, `.font-serif` lignes 108-112, `.cmtl-verdict` lignes 117-123)
- Modify: `src/components/ShopSheet.tsx:75` (classe du h2), `src/components/RatingCookies.tsx:11` (classe du chiffre), `src/components/admin/AdminApp.tsx:152` (h1), `src/components/CookieMap.tsx:356` (puce langue, classe `font-mono`)

**Interfaces:**
- Consumes: `src/fonts/gill-sans-ultra-bold.otf` (déjà présent).
- Produces: variables CSS `--font-title` et `--font-body` sur `<html>` ; utilitaire Tailwind `font-display` (via token `--font-display`) utilisé par toutes les tâches suivantes pour les titres.

- [ ] **Step 1 : Remplacer les fonts dans `layout.tsx`**

Supprimer les imports/consts `Fraunces`, `Geist`, `Geist_Mono` et les remplacer par :

```tsx
import type { Metadata } from "next";
import localFont from "next/font/local";
import { Comfortaa } from "next/font/google";
import "./globals.css";
import { LangProvider } from "@/components/LangProvider";

// Titres et marque : la Gill Sans Ultra Bold fournie par Léo (licence perso, self-host).
const gillSans = localFont({
  src: "../fonts/gill-sans-ultra-bold.otf",
  variable: "--font-title",
  weight: "700",
  display: "swap",
});

const comfortaa = Comfortaa({
  variable: "--font-body",
  subsets: ["latin"],
  display: "swap",
});
```

et sur `<html>` : `className={`${gillSans.variable} ${comfortaa.variable} h-full antialiased`}`.

- [ ] **Step 2 : Recâbler `globals.css`**

Remplacer le bloc `@theme inline` (lignes 91-97) par :

```css
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-body);
  --font-display: var(--font-title);
}
```

`body` : `font-family: var(--font-body), ui-rounded, ui-sans-serif, system-ui, sans-serif;`

Remplacer le bloc `.font-serif` (lignes 106-112) par :

```css
/* La Gill Sans Ultra Bold est déjà très noire : on la laisse respirer. */
.font-display {
  letter-spacing: 0.01em;
}
```

Remplacer `.cmtl-verdict` (lignes 114-123) par :

```css
/* L'avis reste la voix du site : Comfortaa en italique synthétique, un cran au-dessus. */
.cmtl-verdict {
  font-family: var(--font-body), ui-rounded, system-ui, sans-serif;
  font-style: italic;
  font-weight: 500;
  letter-spacing: -0.005em;
}
```

- [ ] **Step 3 : Basculer les usages**

- `ShopSheet.tsx:75` : `className="font-serif pr-9 …"` → `className="font-display pr-9 …"`.
- `RatingCookies.tsx:11` : `font-serif` → supprimer la classe (le chiffre passe en Comfortaa héritée).
- `AdminApp.tsx:152` : `className="font-serif text-[26px] …"` → `className="font-display text-[24px] …"` (l'Ultra Bold est plus massive, un cran de moins). Ne PAS toucher au contenu `🍪 Admin` ici (Task 10).
- `CookieMap.tsx:356` (puce EN/FR) : retirer `font-mono` de la className, garder `font-medium tracking-[0.14em]`.
- Vérifier qu'aucun autre usage ne reste : `grep -rn "font-serif\|font-mono\|fraunces\|geist" src/` doit ne montrer que des commentaires (les nettoyer aussi).

- [ ] **Step 4 : Vérifier**

Run: `npm test` — Expected: PASS (aucun test ne cible les fonts).
Run: `npx eslint src` — Expected: pas de nouvelle erreur (imports morts supprimés).

- [ ] **Step 5 : Commit**

```bash
git add src/app/layout.tsx src/app/globals.css src/components/ShopSheet.tsx src/components/RatingCookies.tsx src/components/admin/AdminApp.tsx src/components/CookieMap.tsx src/fonts/gill-sans-ultra-bold.otf
git commit -m "feat: typographies de marque — Gill Sans Ultra Bold (titres) + Comfortaa (body)"
```

---

### Task 2 : `src/lib/site.ts` + renommage Cookies Club — Montréal

**Files:**
- Create: `src/lib/site.ts`
- Test: `src/lib/__tests__/site.test.ts`
- Modify: `src/app/layout.tsx` (bloc `metadata`), `src/app/manifest.ts`, `src/components/RasterMap.tsx:65` (attribution), `src/app/c/[slug]/page.tsx` (fallbacks « Cookies MTL » — la refonte complète des metadata vient en Task 14), `README.md` (titre)

**Interfaces:**
- Produces: `SITE_URL: string`, `SITE_NAME = 'Cookies Club — Montréal'`, `SITE_BRAND = 'Cookies Club'`, `SITE_TITLE: string`, `SITE_DESCRIPTION: string`, `shopTitle(name: string): string`, `shopDescription(s: { rating: number; address: string; review: string }): string` — consommés par les Tasks 14-17.

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// src/lib/__tests__/site.test.ts
import { expect, test } from 'vitest'
import { SITE_NAME, SITE_TITLE, SITE_URL, SITE_DESCRIPTION, shopTitle, shopDescription } from '../site'

test('le nom textuel du site est Cookies Club — Montréal', () => {
  expect(SITE_NAME).toBe('Cookies Club — Montréal')
  expect(SITE_URL).toBe('https://cookies.zucchinistudio.com')
})

test('le title couvre les requêtes FR et EN', () => {
  expect(SITE_TITLE).toContain('Cookies Club — Montréal')
  expect(SITE_TITLE.toLowerCase()).toContain('cookie map')
  expect(SITE_DESCRIPTION).toContain('MTL')
})

test('titre et description de fiche', () => {
  expect(shopTitle('Félix & Norton')).toBe('Félix & Norton — Cookies Club Montréal')
  const d = shopDescription({ rating: 4.5, address: '123 rue Rachel E', review: 'Croustillant dehors, fondant dedans.' })
  expect(d).toContain('4,5')
  expect(d).toContain('123 rue Rachel E')
})
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/lib/__tests__/site.test.ts` — Expected: FAIL (module inexistant).

- [ ] **Step 3 : Implémenter `src/lib/site.ts`**

```ts
// Identité textuelle et domaine — source unique pour metadata, manifest, sitemap, JSON-LD.
// La marque visuelle (logo) reste « Cookies Club » ; le textuel porte « — Montréal »
// pour le SEO local (requêtes : cookie map, cookie montréal, cookies mtl…).
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://cookies.zucchinistudio.com'
export const SITE_NAME = 'Cookies Club — Montréal'
export const SITE_BRAND = 'Cookies Club'
export const SITE_TITLE = "Cookies Club — Montréal · La carte des cookies · Montreal's cookie map"
export const SITE_DESCRIPTION =
  'Les meilleurs cookies de Montréal (MTL), goûtés, notés et cartographiés. ' +
  'The best cookies in Montreal, tasted, rated and mapped by Cookies Club.'

export function shopTitle(name: string): string {
  return `${name} — Cookies Club Montréal`
}

export function shopDescription(s: { rating: number; address: string; review: string }): string {
  const note = String(s.rating).replace('.', ',')
  return `Note ${note}/5 · ${s.address} · L'avis Cookies Club — Montréal (MTL) cookie review: ${s.review.slice(0, 100)}`
}
```

- [ ] **Step 4 : Vérifier le vert**

Run: `npx vitest run src/lib/__tests__/site.test.ts` — Expected: PASS.

- [ ] **Step 5 : Propager le renommage**

- `layout.tsx` : `metadata` devient (import depuis `@/lib/site`) :

```tsx
export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: SITE_TITLE,
  description: SITE_DESCRIPTION,
};
```

- `manifest.ts` : `name: SITE_NAME`, `short_name: SITE_BRAND`, `description: 'La carte des cookies de Montréal · Montreal cookie map'` (imports depuis `@/lib/site`).
- `RasterMap.tsx:65` : `… · style Cookies MTL` → `… · style Cookies Club`.
- `c/[slug]/page.tsx` : les deux `'Cookies MTL'` / `` `${shop.name} — Cookies MTL` `` → `SITE_NAME` / `shopTitle(shop.name)` (la description complète attendra la Task 14).
- `README.md` : « Cookies MTL » du titre → « Cookies Club — Montréal (Cookies MTL) » (garder l'ancien nom en mention pour l'historique).
- `grep -rn "Cookies MTL" src/` → ne doit rester que des commentaires de code (acceptables).

- [ ] **Step 6 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/lib/site.ts src/lib/__tests__/site.test.ts src/app/layout.tsx src/app/manifest.ts src/components/RasterMap.tsx "src/app/c/[slug]/page.tsx" README.md
git commit -m "feat: renommage Cookies Club — Montréal + src/lib/site.ts (identité unique)"
```

---

### Task 3 : Thème — `src/lib/theme.ts`, script anti-FOUC, CSS `[data-theme]`

**Files:**
- Create: `src/lib/theme.ts`
- Test: `src/lib/__tests__/theme.test.ts`
- Modify: `src/app/layout.tsx` (script inline en tête de `<body>`), `src/app/globals.css` (4 blocs `@media (prefers-color-scheme: dark)` : lignes 55-89, 181-186, 247-252, 279-284), `src/lib/map-style.ts:14-17` (`currentTheme`)

**Interfaces:**
- Produces: `type Theme = 'light' | 'dark'`, `THEME_KEY = 'cmtl_theme'`, `systemTheme(): Theme`, `storedTheme(): Theme | null`, `resolveTheme(): Theme`, `applyTheme(t: Theme): void` (stampe `<html data-theme>`, persiste, dispatch), `toggleTheme(): void`, `onThemeChange(cb: (t: Theme) => void): () => void` (désabonnement), `THEME_INIT_SCRIPT: string`. Consommés par les Tasks 5, 6.

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
// src/lib/__tests__/theme.test.ts
import { afterEach, expect, test, vi } from 'vitest'
import { applyTheme, onThemeChange, resolveTheme, storedTheme, toggleTheme, THEME_INIT_SCRIPT, THEME_KEY } from '../theme'

afterEach(() => {
  localStorage.clear()
  delete document.documentElement.dataset.theme
})

test('resolveTheme : localStorage prime sur le système', () => {
  localStorage.setItem(THEME_KEY, 'dark')
  expect(resolveTheme()).toBe('dark')
  localStorage.setItem(THEME_KEY, 'nimporte-quoi')
  expect(storedTheme()).toBeNull() // valeur inconnue ignorée
})

test('applyTheme stampe html, persiste et notifie', () => {
  const seen: string[] = []
  const off = onThemeChange((t) => seen.push(t))
  applyTheme('dark')
  expect(document.documentElement.dataset.theme).toBe('dark')
  expect(localStorage.getItem(THEME_KEY)).toBe('dark')
  expect(seen).toEqual(['dark'])
  off()
  applyTheme('light')
  expect(seen).toEqual(['dark']) // désabonné
})

test('toggleTheme inverse le thème résolu', () => {
  applyTheme('light')
  toggleTheme()
  expect(document.documentElement.dataset.theme).toBe('dark')
})

test('le script inline stampe html avant peinture', () => {
  localStorage.setItem(THEME_KEY, 'dark')
  new Function(THEME_INIT_SCRIPT)()
  expect(document.documentElement.dataset.theme).toBe('dark')
})
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/lib/__tests__/theme.test.ts` — Expected: FAIL (module inexistant).

- [ ] **Step 3 : Implémenter `src/lib/theme.ts`**

```ts
// Thème manuel clair/sombre (spec v1.1) : `data-theme` sur <html>, résolu
// localStorage > système. Tout le CSS et currentTheme() lisent cet attribut ;
// les composants réactifs s'abonnent via onThemeChange.
export type Theme = 'light' | 'dark'
export const THEME_KEY = 'cmtl_theme'
const EVENT = 'cmtl-theme-change'

export function systemTheme(): Theme {
  if (typeof window === 'undefined' || typeof window.matchMedia !== 'function') return 'light'
  return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
}

export function storedTheme(): Theme | null {
  try {
    const v = localStorage.getItem(THEME_KEY)
    return v === 'light' || v === 'dark' ? v : null
  } catch {
    return null // localStorage indisponible (Safari privé…) : on suit le système
  }
}

export function resolveTheme(): Theme {
  return storedTheme() ?? systemTheme()
}

export function applyTheme(theme: Theme): void {
  document.documentElement.dataset.theme = theme
  try {
    localStorage.setItem(THEME_KEY, theme)
  } catch {
    /* choix non persisté, la session courante reste cohérente */
  }
  window.dispatchEvent(new CustomEvent<Theme>(EVENT, { detail: theme }))
}

export function toggleTheme(): void {
  applyTheme(resolveTheme() === 'dark' ? 'light' : 'dark')
}

export function onThemeChange(cb: (t: Theme) => void): () => void {
  const handler = (e: Event) => cb((e as CustomEvent<Theme>).detail)
  window.addEventListener(EVENT, handler)
  return () => window.removeEventListener(EVENT, handler)
}

// Exécuté inline en premier enfant de <body> : stampe le thème résolu avant la
// première peinture (anti-FOUC). Doit rester autonome (pas d'import).
export const THEME_INIT_SCRIPT = `(function(){try{var s=localStorage.getItem('${THEME_KEY}');var t=(s==='light'||s==='dark')?s:(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');document.documentElement.dataset.theme=t;}catch(e){document.documentElement.dataset.theme='light';}})()`
```

- [ ] **Step 4 : Vérifier le vert**

Run: `npx vitest run src/lib/__tests__/theme.test.ts` — Expected: PASS.

- [ ] **Step 5 : Brancher le script dans `layout.tsx`**

Premier enfant de `<body>` :

```tsx
<body className="min-h-full flex flex-col">
  <script dangerouslySetInnerHTML={{ __html: THEME_INIT_SCRIPT }} />
  <LangProvider>{children}</LangProvider>
</body>
```

(import `THEME_INIT_SCRIPT` depuis `@/lib/theme`).

- [ ] **Step 6 : Migrer `globals.css` vers `[data-theme]`**

Les 4 blocs `@media (prefers-color-scheme: dark) { X }` deviennent des sélecteurs directs (le script stampe toujours l'attribut, plus besoin du media query) :

- lignes 55-89 : `@media … { :root { … } }` → `:root[data-theme='dark'] { color-scheme: dark; …mêmes tokens… }`
- lignes 181-186 : → `:root[data-theme='dark'] .cmtl-pin { … }`
- lignes 247-252 : → `:root[data-theme='dark'] .maplibregl-ctrl-group button .maplibregl-ctrl-icon { … }`
- lignes 279-284 : → `:root[data-theme='dark'] .maplibregl-ctrl-attrib-button { … }`

Aucune valeur de token ne change — uniquement les sélecteurs.

- [ ] **Step 7 : `currentTheme()` lit `data-theme` d'abord**

Dans `src/lib/map-style.ts`, remplacer le corps de `currentTheme` :

```ts
// Source de vérité du thème pour les cartes : l'attribut posé par src/lib/theme.ts
// (toggle manuel), sinon le système. Fallback 'light' hors navigateur (jsdom, SSR).
export function currentTheme(): MapTheme {
  if (typeof document !== 'undefined') {
    const t = document.documentElement.dataset.theme
    if (t === 'light' || t === 'dark') return t
  }
  if (typeof window === 'undefined' || typeof window.matchMedia !== 'function') return 'light'
  return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
}
```

- [ ] **Step 8 : Vérifier et committer**

Run: `npm test` — Expected: PASS (les tests carte existants passent, `currentTheme` garde son fallback jsdom).

```bash
git add src/lib/theme.ts src/lib/__tests__/theme.test.ts src/app/layout.tsx src/app/globals.css src/lib/map-style.ts
git commit -m "feat: thème manuel data-theme — lib, script anti-FOUC, CSS et currentTheme"
```

---

### Task 4 : Icônes SVG communes + remplacement des caractères décoratifs

**Files:**
- Create: `src/components/icons.tsx`
- Test: `src/components/__tests__/icons.test.tsx`
- Modify: `src/components/ShopSheet.tsx:64` (✕), `src/components/ShopSheet.tsx:113` (↗), `src/components/RasterMap.tsx:98` (◎)

**Interfaces:**
- Produces: composants `IconSun, IconMoon, IconClose, IconExternal, IconLocate, IconDirections, IconCopy, IconShare, IconCheck`, tous `({ size = 16 }: { size?: number })`, SVG `stroke="currentColor" strokeWidth={2} fill="none" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"`. Consommés par les Tasks 5, 10, 12, 13.

- [ ] **Step 1 : Écrire le test qui échoue**

```tsx
// src/components/__tests__/icons.test.tsx
import { expect, test } from 'vitest'
import { render } from '@testing-library/react'
import { IconCheck, IconClose, IconCopy, IconDirections, IconExternal, IconLocate, IconMoon, IconShare, IconSun } from '../icons'

test('chaque icône rend un svg décoratif en trait', () => {
  for (const Icon of [IconSun, IconMoon, IconClose, IconExternal, IconLocate, IconDirections, IconCopy, IconShare, IconCheck]) {
    const { container, unmount } = render(<Icon />)
    const svg = container.querySelector('svg')
    expect(svg?.getAttribute('aria-hidden')).toBe('true')
    expect(svg?.getAttribute('stroke')).toBe('currentColor')
    expect(svg?.getAttribute('fill')).toBe('none')
    unmount()
  }
})
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/components/__tests__/icons.test.tsx` — Expected: FAIL (module inexistant).

- [ ] **Step 3 : Implémenter `src/components/icons.tsx`**

Tracés style Feather (24×24), sobres :

```tsx
// Icônes SVG en trait — la règle du projet : jamais d'emoji ni de caractère
// décoratif dans l'UI. stroke=currentColor pour hériter de la couleur du texte.
type IconProps = { size?: number }

function base(size: number) {
  return {
    width: size,
    height: size,
    viewBox: '0 0 24 24',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 2,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    'aria-hidden': 'true',
  } as const
}

export function IconSun({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <circle cx="12" cy="12" r="4" />
      <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41" />
    </svg>
  )
}

export function IconMoon({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z" />
    </svg>
  )
}

export function IconClose({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <path d="M18 6 6 18M6 6l12 12" />
    </svg>
  )
}

export function IconExternal({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
      <path d="M15 3h6v6M10 14 21 3" />
    </svg>
  )
}

export function IconLocate({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <circle cx="12" cy="12" r="3" />
      <path d="M12 2v3M12 19v3M2 12h3M19 12h3" />
    </svg>
  )
}

export function IconDirections({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <path d="M9 20V10a2 2 0 0 1 2-2h8" />
      <path d="m15 4 4 4-4 4" />
    </svg>
  )
}

export function IconCopy({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <rect x="9" y="9" width="12" height="12" rx="2" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  )
}

export function IconShare({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <circle cx="18" cy="5" r="3" />
      <circle cx="6" cy="12" r="3" />
      <circle cx="18" cy="19" r="3" />
      <path d="m8.6 13.5 6.8 4M15.4 6.5l-6.8 4" />
    </svg>
  )
}

export function IconCheck({ size = 16 }: IconProps) {
  return (
    <svg {...base(size)}>
      <path d="M20 6 9 17l-5-5" />
    </svg>
  )
}
```

- [ ] **Step 4 : Vérifier le vert**

Run: `npx vitest run src/components/__tests__/icons.test.tsx` — Expected: PASS.

- [ ] **Step 5 : Remplacer les caractères décoratifs existants**

- `ShopSheet.tsx:64` : `✕` → `<IconClose />` (le bouton garde son `aria-label={t('close')}`).
- `ShopSheet.tsx:113` : `{t('googleListing')} ↗` → `<span className="inline-flex items-center gap-1">{t('googleListing')} <IconExternal size={13} /></span>`.
- `RasterMap.tsx:98` : `btn.textContent = '◎'` → `btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg>'` (hors React, markup direct — même tracé qu'`IconLocate`).
- Dans `i18n.ts`, retirer les ✓ : `copied: 'Copié ✓'` → `'Copié'`, `linkCopied: 'Lien copié ✓'` → `'Lien copié'` (idem en anglais) — la coche devient une icône en Task 12.

- [ ] **Step 6 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/components/icons.tsx src/components/__tests__/icons.test.tsx src/components/ShopSheet.tsx src/components/RasterMap.tsx src/lib/i18n.ts
git commit -m "feat: icônes SVG en trait — plus aucun caractère décoratif dans l'UI"
```

---

### Task 5 : ThemeToggle + intégration dans la carte

**Files:**
- Create: `src/components/ThemeToggle.tsx`
- Test: `src/components/__tests__/theme-toggle.test.tsx`
- Modify: `src/components/CookieMap.tsx` (sous la puce EN/FR, lignes 353-361), `src/lib/i18n.ts` (2 clés)

**Interfaces:**
- Consumes: `resolveTheme`, `toggleTheme`, `onThemeChange` (Task 3) ; `IconSun`, `IconMoon` (Task 4).
- Produces: `<ThemeToggle className?: string>` — bouton autonome, rend `null` avant hydratation.

- [ ] **Step 1 : Ajouter les clés i18n**

Dans `dict.fr` : `themeToDark: 'Passer en mode sombre'`, `themeToLight: 'Passer en mode clair'` ; dans `dict.en` : `'Switch to dark mode'`, `'Switch to light mode'`.

- [ ] **Step 2 : Écrire le test qui échoue**

```tsx
// src/components/__tests__/theme-toggle.test.tsx
import { afterEach, expect, test } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'
import { LangProvider } from '../LangProvider'
import { ThemeToggle } from '../ThemeToggle'

afterEach(() => {
  localStorage.clear()
  delete document.documentElement.dataset.theme
})

test('bascule le thème et son propre libellé', () => {
  document.documentElement.dataset.theme = 'light'
  render(
    <LangProvider>
      <ThemeToggle />
    </LangProvider>
  )
  const btn = screen.getByLabelText('Passer en mode sombre')
  fireEvent.click(btn)
  expect(document.documentElement.dataset.theme).toBe('dark')
  expect(screen.getByLabelText('Passer en mode clair')).toBeDefined()
})
```

- [ ] **Step 3 : Vérifier l'échec**

Run: `npx vitest run src/components/__tests__/theme-toggle.test.tsx` — Expected: FAIL (module inexistant).

- [ ] **Step 4 : Implémenter `src/components/ThemeToggle.tsx`**

```tsx
'use client'

import { useEffect, useState } from 'react'
import { onThemeChange, resolveTheme, toggleTheme, type Theme } from '@/lib/theme'
import { useLang } from './LangProvider'
import { IconMoon, IconSun } from './icons'

// Rendu null avant montage : le SSR ne connaît pas le thème résolu, et rendre un
// état arbitraire créerait un mismatch d'hydratation (même motif que le renderer
// dans CookieMap).
export function ThemeToggle({ className }: { className?: string }) {
  const [theme, setTheme] = useState<Theme | null>(null)
  const { t } = useLang()
  useEffect(() => {
    // eslint-disable-next-line react-hooks/set-state-in-effect
    setTheme(resolveTheme())
    return onThemeChange(setTheme)
  }, [])
  if (!theme) return null
  const label = theme === 'dark' ? t('themeToLight') : t('themeToDark')
  return (
    <button onClick={toggleTheme} aria-label={label} title={label} className={className}>
      {theme === 'dark' ? <IconSun /> : <IconMoon />}
    </button>
  )
}
```

- [ ] **Step 5 : Vérifier le vert**

Run: `npx vitest run src/components/__tests__/theme-toggle.test.tsx` — Expected: PASS.

- [ ] **Step 6 : Intégrer dans `CookieMap.tsx`**

Juste après le bouton de langue (ligne 361), même famille de classes, empilé dessous :

```tsx
<ThemeToggle className="absolute right-3 top-[calc(0.75rem+env(safe-area-inset-top)+44px)] z-10 flex h-[34px] w-[46px] items-center justify-center rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] text-[color:var(--text-body)] shadow-[var(--shadow-chip)] transition-colors hover:bg-[color:var(--surface-2)]" />
```

- [ ] **Step 7 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/components/ThemeToggle.tsx src/components/__tests__/theme-toggle.test.tsx src/components/CookieMap.tsx src/lib/i18n.ts
git commit -m "feat: puce de bascule sombre/clair sous la puce de langue"
```

---

### Task 6 : Les deux cartes suivent le toggle

**Files:**
- Modify: `src/components/CookieMap.tsx` (nouvel effet d'abonnement), `src/components/RasterMap.tsx` (ref du tileLayer + effet)
- Test: `src/components/__tests__/map-theme-switch.test.tsx`

**Interfaces:**
- Consumes: `onThemeChange` (Task 3), `getRecoloredStyle` (interne à CookieMap), `getMapStyleUrl` (existant).
- Produces: comportement — `applyTheme('dark')` recharge le style MapLibre (`map.setStyle`) et bascule l'URL des tuiles Leaflet, sans démonter les cartes ni perdre la caméra.

- [ ] **Step 1 : Écrire le test qui échoue**

S'inspirer du mock maplibre de `cookie-map-raster-switch.test.tsx` (le lire d'abord ; réutiliser ses helpers de mock au besoin). Nouveau fichier :

```tsx
// src/components/__tests__/map-theme-switch.test.tsx
// Vérifie le contrat de la Task 6 : un changement de thème appelle map.setStyle
// avec le style recoloré du nouveau thème, sans recréer la Map.
import { afterEach, beforeEach, expect, test, vi } from 'vitest'
import { act, render } from '@testing-library/react'

const setStyle = vi.fn()
const mapInstances: unknown[] = []
vi.mock('maplibre-gl', () => {
  class FakeMap {
    constructor() { mapInstances.push(this) }
    on() { return this }
    once() { return this }
    addControl() {}
    remove() {}
    getCanvas() { return { addEventListener() {}, removeEventListener() {} } }
    getCenter() { return { lng: -73.5674, lat: 45.5019 } }
    getZoom() { return 12 }
    easeTo() {}
    setStyle
  }
  class FakeMarker {
    setLngLat() { return this }
    addTo() { return this }
    remove() {}
  }
  return { Map: FakeMap, Marker: FakeMarker, GeolocateControl: class {}, NavigationControl: class {} }
})

beforeEach(() => {
  vi.stubGlobal('fetch', vi.fn(async () => ({ ok: true, json: async () => ({ layers: [] }) })))
})
afterEach(() => {
  vi.unstubAllGlobals()
  localStorage.clear()
  delete document.documentElement.dataset.theme
  setStyle.mockClear()
  mapInstances.length = 0
})

test('applyTheme fait suivre la carte MapLibre sans la recréer', async () => {
  const { CookieMap, __clearStyleCacheForTests } = await import('../CookieMap')
  const { applyTheme } = await import('@/lib/theme')
  __clearStyleCacheForTests()
  document.documentElement.dataset.theme = 'light'
  render(<CookieMap shops={[]} />)
  await act(async () => { await Promise.resolve() }) // laisse init() poser la carte
  const created = mapInstances.length
  await act(async () => {
    applyTheme('dark')
    await Promise.resolve()
  })
  expect(setStyle).toHaveBeenCalledTimes(1)
  expect(mapInstances.length).toBe(created) // pas de rebuild
})
```

(Si le mock existant de `cookie-map-raster-switch.test.tsx` diffère — le calquer : c'est LUI la référence des méthodes à mocker. L'important est le contrat : `setStyle` appelé, pas de nouvelle instance.)

- [ ] **Step 2 : Vérifier l'échec**

Run: `npx vitest run src/components/__tests__/map-theme-switch.test.tsx` — Expected: FAIL (`setStyle` jamais appelé).

- [ ] **Step 3 : Implémenter dans `CookieMap.tsx`**

Après l'effet principal, nouvel effet indépendant :

```tsx
// Le toggle thème recharge le style sans démonter la carte : la caméra, les
// marqueurs DOM et l'état de sélection survivent au changement.
useEffect(() => {
  return onThemeChange((theme) => {
    const map = mapRef.current
    if (!map) return
    getRecoloredStyle(theme, getMapStyleUrl(theme))
      .then((style) => {
        if (mapRef.current === map) map.setStyle(style)
      })
      .catch(() => {
        /* le style courant reste affiché ; le prochain toggle retentera */
      })
  })
}, [])
```

Et dans `warmRasterFallback` (ligne 163), remplacer la closure `theme` par `currentTheme()` pour préchauffer les tuiles du thème réellement actif.

- [ ] **Step 4 : Implémenter dans `RasterMap.tsx`**

- Ajouter `const tileLayerRef = useRef<import('leaflet').TileLayer | null>(null)` ; dans l'init, capturer : `tileLayerRef.current = L.tileLayer(…)` (avant le `.addTo(map)`, en gardant le chaînage : `const layer = L.tileLayer(…); tileLayerRef.current = layer; layer.addTo(map)`).
- Nouvel effet :

```tsx
// Bascule de pyramide au toggle (les deux thèmes de tuiles sont pré-rendus).
useEffect(() => {
  return onThemeChange((theme) => {
    tileLayerRef.current?.setUrl(`${TILES_BASE}/tiles/v1/${theme}/{z}/{x}/{y}.webp`)
  })
}, [])
```

- [ ] **Step 5 : Vérifier et committer**

Run: `npm test` — Expected: PASS (dont `raster-map.test.tsx` et `cookie-map-*.test.tsx` existants).

```bash
git add src/components/CookieMap.tsx src/components/RasterMap.tsx src/components/__tests__/map-theme-switch.test.tsx
git commit -m "feat: les cartes MapLibre et Leaflet suivent le toggle de thème à chaud"
```

---

### Task 7 : Sprite des cookies de marque + RatingCookies en SVG

**Files:**
- Create: `src/components/CookieSprite.tsx` (généré depuis les SVG optimisés)
- Modify: `src/components/RatingCookies.tsx` (réécriture), `src/app/layout.tsx` (montage du sprite), tests `src/components/__tests__/rating.test.tsx`
- Source assets: `public/brand/full cookie note.svg`, `public/brand/demi cookie note.svg`, `public/brand/contour cookie.svg` (lecture seule — ne pas les modifier)

**Interfaces:**
- Produces: `<CookieSprite />` (symbols `#cmtl-cookie-full`, `#cmtl-cookie-half`, `#cmtl-cookie-empty`, viewBox `0 0 300 300`, monté une fois dans le layout) ; `RatingCookies({ rating, size = 'sm' })` avec `size?: 'sm' | 'lg'` (18 px / 32 px par cookie) et `data-cookie="full|half|empty"` sur chaque `<svg>` (hooks de test). Consommé par les Tasks 8, 9, 17.

- [ ] **Step 1 : Optimiser les 3 SVG (une fois, vers le scratchpad)**

```bash
cd /c/Users/leova/Documents/Projets/cookies-mtl
S="$TMPDIR/cookie-svg"; mkdir -p "$S"
cat > "$S/svgo.config.mjs" <<'EOF'
export default {
  multipass: true,
  plugins: [
    'preset-default',
    'convertStyleToAttrs',
    { name: 'prefixIds', params: { prefixClassNames: true } },
  ],
}
EOF
npx svgo --config "$S/svgo.config.mjs" -i "public/brand/full cookie note.svg" -o "$S/full.svg"
npx svgo --config "$S/svgo.config.mjs" -i "public/brand/demi cookie note.svg" -o "$S/half.svg"
npx svgo --config "$S/svgo.config.mjs" -i "public/brand/contour cookie.svg" -o "$S/empty.svg"
wc -c "$S"/*.svg
```

Attendu : chaque fichier optimisé sous ~15 Ko (bruts : 39-52 Ko). `prefixIds` évite les collisions d'ids/classes entre les trois symbols ; `convertStyleToAttrs` élimine les `<style>` internes (interdits en sprite partagé).

- [ ] **Step 2 : Générer `src/components/CookieSprite.tsx`**

Composant serveur (pas de `'use client'`). Pour chacun des 3 fichiers optimisés, copier le CONTENU INTERNE du `<svg>` (pas la balise racine) dans un `<symbol>`, en convertissant les attributs en camelCase JSX (`clip-path` → `clipPath`, etc. — utiliser une conversion mécanique, p. ex. coller dans le fichier puis corriger les erreurs TS listées par `npx tsc --noEmit`) :

```tsx
// Sprite des cookies de marque (public/brand, optimisés svgo — ne pas éditer à la
// main). Monté une fois dans le layout ; consommé par <use href="#cmtl-cookie-*">
// dans RatingCookies ET dans les marqueurs des deux cartes.
export function CookieSprite() {
  return (
    <svg width="0" height="0" style={{ position: 'absolute' }} aria-hidden="true" focusable="false">
      <defs>
        <symbol id="cmtl-cookie-full" viewBox="0 0 300 300">{/* contenu de full.svg */}</symbol>
        <symbol id="cmtl-cookie-half" viewBox="0 0 300 300">{/* contenu de half.svg */}</symbol>
        <symbol id="cmtl-cookie-empty" viewBox="0 0 300 300">{/* contenu de empty.svg */}</symbol>
      </defs>
    </svg>
  )
}
```

Monter dans `layout.tsx`, juste après le script de thème : `<CookieSprite />`.

- [ ] **Step 3 : Mettre à jour le test (il doit échouer)**

Réécrire `src/components/__tests__/rating.test.tsx` :

```tsx
import { expect, test } from 'vitest'
import { render, screen } from '@testing-library/react'
import { RatingCookies } from '../RatingCookies'

test('3,5 → 3 pleins, 1 demi, 1 contour, label accessible', () => {
  const { container } = render(<RatingCookies rating={3.5} />)
  expect(screen.getByLabelText('3,5 / 5')).toBeDefined()
  expect(container.querySelectorAll('[data-cookie="full"]')).toHaveLength(3)
  expect(container.querySelectorAll('[data-cookie="half"]')).toHaveLength(1)
  expect(container.querySelectorAll('[data-cookie="empty"]')).toHaveLength(1)
  expect(container.textContent).toContain('3,5')
})

test('bornes 0 et 5', () => {
  const zero = render(<RatingCookies rating={0} />)
  expect(zero.container.querySelectorAll('[data-cookie="empty"]')).toHaveLength(5)
  const five = render(<RatingCookies rating={5} />)
  expect(five.container.querySelectorAll('[data-cookie="full"]')).toHaveLength(5)
})

test('size lg grossit les cookies (admin)', () => {
  const { container } = render(<RatingCookies rating={4} size="lg" />)
  expect(container.querySelector('svg')?.getAttribute('width')).toBe('32')
})
```

Run: `npx vitest run src/components/__tests__/rating.test.tsx` — Expected: FAIL (les points ● n'ont pas de `data-cookie`).

- [ ] **Step 4 : Réécrire `RatingCookies.tsx`**

```tsx
export function RatingCookies({ rating, size = 'sm' }: { rating: number; size?: 'sm' | 'lg' }) {
  const px = size === 'lg' ? 32 : 18
  const label = `${String(rating).replace('.', ',')} / 5`
  const kinds = Array.from({ length: 5 }, (_, i) => {
    if (rating >= i + 1) return 'full' as const
    if (rating >= i + 0.5) return 'half' as const
    return 'empty' as const
  })
  return (
    <span aria-label={label} className="inline-flex items-center gap-2.5">
      <span aria-hidden className="inline-flex items-center gap-1">
        {kinds.map((kind, i) => (
          <svg key={i} width={px} height={px} viewBox="0 0 300 300" data-cookie={kind}>
            <use href={`#cmtl-cookie-${kind}`} />
          </svg>
        ))}
      </span>
      <span aria-hidden className="text-[14px] font-semibold leading-none text-[color:var(--text-muted)]">
        {String(rating).replace('.', ',')}
      </span>
    </span>
  )
}
```

- [ ] **Step 5 : Vérifier et committer**

Run: `npm test` — Expected: PASS (dont `shop-sheet-ssr.test.tsx` qui rend RatingCookies via ShopSheet).

```bash
git add src/components/CookieSprite.tsx src/components/RatingCookies.tsx src/components/__tests__/rating.test.tsx src/app/layout.tsx
git commit -m "feat: notes en cookies SVG de la marque (sprite plein/demi/contour)"
```

---

### Task 8 : RatingInput admin — slider + visualisation

**Files:**
- Modify: `src/components/admin/RatingInput.tsx` (réécriture complète), test `src/components/admin/__tests__/rating-input.test.tsx` (réécriture)

**Interfaces:**
- Consumes: `RatingCookies` avec `size="lg"` (Task 7).
- Produces: même API `RatingInput({ value, onChange }: { value: number; onChange: (v: number) => void })` — AdminApp (ligne 201) ne change pas. `data-testid="rating-slider"` sur l'input range.

- [ ] **Step 1 : Réécrire le test (il doit échouer)**

```tsx
// src/components/admin/__tests__/rating-input.test.tsx
import { expect, test, vi } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'
import { RatingInput } from '../RatingInput'

test('slider 0→5 par pas de 0,5, câblé à onChange', () => {
  const onChange = vi.fn()
  render(<RatingInput value={3} onChange={onChange} />)
  const slider = screen.getByTestId('rating-slider') as HTMLInputElement
  expect(slider.min).toBe('0')
  expect(slider.max).toBe('5')
  expect(slider.step).toBe('0.5')
  fireEvent.change(slider, { target: { value: '3.5' } })
  expect(onChange).toHaveBeenCalledWith(3.5)
})

test('la visualisation cookies reflète la valeur', () => {
  const { container } = render(<RatingInput value={3.5} onChange={() => {}} />)
  expect(container.querySelectorAll('[data-cookie="full"]')).toHaveLength(3)
  expect(container.querySelectorAll('[data-cookie="half"]')).toHaveLength(1)
  expect(container.textContent).toContain('3,5')
})
```

Run: `npx vitest run src/components/admin/__tests__/rating-input.test.tsx` — Expected: FAIL (l'ancien composant n'a pas de slider).

- [ ] **Step 2 : Réécrire `RatingInput.tsx`**

```tsx
'use client'

import { RatingCookies } from '@/components/RatingCookies'

// Retour QA v1 : les 🍪 à opacité variable rendaient la demi-note illisible.
// Nouveau contrat : un slider (pas de 0,5) + la MÊME visualisation cookies que
// le site public — l'admin voit exactement ce que les visiteurs verront.
export function RatingInput({ value, onChange }: { value: number; onChange: (v: number) => void }) {
  return (
    <div
      role="group"
      aria-label={`Note : ${String(value).replace('.', ',')} / 5`}
      className="flex flex-col gap-3 rounded-[var(--radius-field)] border border-[color:var(--border-strong)] bg-[color:var(--surface-2)] px-4 py-3"
    >
      <RatingCookies rating={value} size="lg" />
      <input
        type="range"
        min={0}
        max={5}
        step={0.5}
        value={value}
        data-testid="rating-slider"
        aria-label="Note sur 5, par demi-point"
        onChange={(e) => onChange(Number(e.target.value))}
        className="h-11 w-full accent-[color:var(--accent)]"
      />
    </div>
  )
}
```

- [ ] **Step 3 : Vérifier et committer**

Run: `npx vitest run src/components/admin` puis `npm test` — Expected: PASS (si d'autres tests admin réfèrent `rating-N-half/full`, les mettre à jour vers le slider avec `fireEvent.change`).

```bash
git add src/components/admin/RatingInput.tsx src/components/admin/__tests__/rating-input.test.tsx
git commit -m "feat: saisie de note admin au slider avec visualisation cookies"
```

---

### Task 9 : Marqueurs cookie sur les deux cartes

**Files:**
- Modify: `src/app/globals.css` (remplacer le bloc `.cmtl-pin`, lignes 137-186), `src/components/CookieMap.tsx:258-267` (création des marqueurs), `src/components/RasterMap.tsx:73-78` (divIcon), tests existants qui sélectionnent `.cmtl-pin` (`grep -rln "cmtl-pin" src/components/__tests__` puis adapter)

**Interfaces:**
- Consumes: sprite `#cmtl-cookie-full` (Task 7, monté globalement par le layout).
- Produces: classe CSS `.cmtl-pin-cookie` (bouton marqueur 34×34) ; helper exporté `cookieMarkerHtml(name: string): string` dans `src/lib/cookie-marker.ts` utilisé par les deux cartes.

- [ ] **Step 1 : Créer `src/lib/cookie-marker.ts` avec son test**

Test d'abord (`src/lib/__tests__/cookie-marker.test.ts`) :

```ts
import { expect, test } from 'vitest'
import { cookieMarkerHtml } from '../cookie-marker'

test('markup du marqueur : bouton accessible + use du sprite', () => {
  const html = cookieMarkerHtml('Chez "Cookie" & Co')
  expect(html).toContain('class="cmtl-pin-cookie"')
  expect(html).toContain('#cmtl-cookie-full')
  expect(html).toContain('aria-label="Chez &quot;Cookie&quot; &amp; Co"')
})
```

Run: `npx vitest run src/lib/__tests__/cookie-marker.test.ts` — Expected: FAIL. Puis :

```ts
// src/lib/cookie-marker.ts
// Markup partagé des marqueurs (MapLibre crée ses éléments DOM à la main,
// Leaflet passe par divIcon.html) : une seule source pour les deux cartes.
function escapeAttr(s: string): string {
  return s.replaceAll('&', '&amp;').replaceAll('"', '&quot;').replaceAll('<', '&lt;')
}

export function cookieMarkerHtml(name: string): string {
  return `<button class="cmtl-pin-cookie" aria-label="${escapeAttr(name)}"><svg viewBox="0 0 300 300" aria-hidden="true"><use href="#cmtl-cookie-full"/></svg></button>`
}
```

Run: PASS.

- [ ] **Step 2 : CSS — remplacer `.cmtl-pin` par `.cmtl-pin-cookie`**

Supprimer les blocs `.cmtl-pin` (lignes 137-186, y compris la variante `[data-theme='dark']` migrée en Task 3) et poser :

```css
/* --- Marqueur cookie ------------------------------------------------------
   Le cookie de la marque, centré sur l'adresse (pas de pointe : ancre centre).
   Le bouton est la cible tactile (34px) ; le svg dessine. */
.cmtl-pin-cookie {
  display: block;
  width: 34px;
  height: 34px;
  padding: 0;
  border: none;
  background: none;
  appearance: none;
  cursor: pointer;
  filter: drop-shadow(0 3px 6px var(--pin-shadow));
  transition: transform 160ms cubic-bezier(0.22, 1, 0.36, 1);
}

.cmtl-pin-cookie svg {
  width: 100%;
  height: 100%;
  display: block;
}

.cmtl-pin-cookie:hover,
.cmtl-pin-cookie:focus-visible {
  transform: translateY(-2px) scale(1.1);
}

.cmtl-pin-cookie:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 3px;
  border-radius: 50%;
}

:root[data-theme='dark'] .cmtl-pin-cookie {
  /* « légèrement lumineux » : le cookie doit décoller du fond chocolat. */
  filter: drop-shadow(0 3px 6px var(--pin-shadow)) drop-shadow(0 0 10px rgba(210, 154, 85, 0.4));
}
```

Garder les tokens `--pin*` de `:root` (le shadow sert encore) ; supprimer ceux devenus morts si `grep -rn "pin-chip\|pin-ring" src` ne trouve plus d'usage.

- [ ] **Step 3 : CookieMap — marqueurs cookie**

Lignes 258-267 : remplacer la création (`el.className = 'cmtl-pin'` etc.) par :

```tsx
const holder = document.createElement('div')
holder.innerHTML = cookieMarkerHtml(shop.name)
const el = holder.firstElementChild as HTMLElement
el.addEventListener('click', (e) => {
  e.stopPropagation()
  setSelected(shop)
  map.easeTo({ center: [shop.lng, shop.lat] })
})
new maplibregl.Marker({ element: el, anchor: 'center' }).setLngLat([shop.lng, shop.lat]).addTo(map)
```

(Reprendre le handler de clic EXISTANT des lignes voisines — s'il fait plus que `setSelected` + `easeTo`, conserver son contenu tel quel ; seuls l'élément et l'ancre changent : `anchor: 'bottom'` → `'center'`.)

- [ ] **Step 4 : RasterMap — divIcon cookie**

Lignes 73-78 :

```tsx
const icon = L.divIcon({
  className: '',
  html: cookieMarkerHtml(shop.name),
  iconSize: [34, 34],
  iconAnchor: [17, 17], // centre : le cookie n'a pas de pointe
})
```

- [ ] **Step 5 : Adapter les tests existants**

`grep -rln "cmtl-pin" src/components/__tests__` — remplacer les sélecteurs `.cmtl-pin` par `.cmtl-pin-cookie` (le contrat bouton-accessible ne change pas).

- [ ] **Step 6 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/lib/cookie-marker.ts src/lib/__tests__/cookie-marker.test.ts src/app/globals.css src/components/CookieMap.tsx src/components/RasterMap.tsx src/components/__tests__
git commit -m "feat: marqueurs cookie de la marque sur les cartes MapLibre et Leaflet"
```

---

### Task 10 : Admin — bouton « Voir la carte » + en-tête sans emoji

**Files:**
- Modify: `src/components/admin/AdminApp.tsx` (header, lignes 151-159)
- Test: `src/components/admin/__tests__/admin-header.test.tsx` (nouveau — si le rendu d'AdminApp exige trop de mocks (db, fetch), tester un sous-composant extrait `AdminHeader`)

**Interfaces:**
- Consumes: `IconExternal` (Task 4).
- Produces: extrait `AdminHeader()` (exporté depuis AdminApp.tsx ou fichier propre `src/components/admin/AdminHeader.tsx` — préférer le fichier propre) : h1 « Admin » + lien « Voir la carte ».

- [ ] **Step 1 : Test qui échoue**

```tsx
// src/components/admin/__tests__/admin-header.test.tsx
import { expect, test } from 'vitest'
import { render, screen } from '@testing-library/react'
import { AdminHeader } from '../AdminHeader'

test('lien Voir la carte vers la carte publique, nouvel onglet', () => {
  render(<AdminHeader />)
  const link = screen.getByRole('link', { name: /voir la carte/i })
  expect(link.getAttribute('href')).toBe('/')
  expect(link.getAttribute('target')).toBe('_blank')
  expect(link.getAttribute('rel')).toContain('noopener')
})

test('le titre est Admin, sans emoji', () => {
  render(<AdminHeader />)
  expect(screen.getByRole('heading').textContent).toBe('Admin')
})
```

Run: Expected FAIL (module inexistant).

- [ ] **Step 2 : Implémenter `src/components/admin/AdminHeader.tsx`**

```tsx
import { IconExternal } from '@/components/icons'

export function AdminHeader() {
  return (
    <div className="flex items-center gap-3">
      <h1 className="font-display text-[24px] leading-none text-[color:var(--text-strong)]">Admin</h1>
      <a
        href="/"
        target="_blank"
        rel="noopener noreferrer"
        className="inline-flex items-center gap-1.5 rounded-full border border-[color:var(--border-strong)] px-3.5 py-2 text-[13px] text-[color:var(--text-body)] transition-colors hover:bg-[color:var(--surface-2)]"
      >
        <IconExternal size={14} />
        Voir la carte
      </a>
    </div>
  )
}
```

Dans `AdminApp.tsx` : remplacer le `<h1>🍪 Admin</h1>` par `<AdminHeader />` (le reste du `<header>` — bouton de déconnexion etc. — ne bouge pas).

- [ ] **Step 3 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/components/admin/AdminHeader.tsx src/components/admin/AdminApp.tsx src/components/admin/__tests__/admin-header.test.tsx
git commit -m "feat: en-tête admin — lien Voir la carte, titre sans emoji"
```

---

### Task 11 : Habillage variante A — logo + crédit sur la carte

**Files:**
- Create: `src/components/BrandLogo.tsx` (SVG du logo inliné), `src/components/MapChrome.tsx` (pastille + crédit)
- Test: `src/components/__tests__/map-chrome.test.tsx`
- Modify: `src/components/CookieMap.tsx` (montage dans le wrapper, visible dans les DEUX modes de rendu)

**Interfaces:**
- Consumes: `public/brand/logo.svg` (source, texte vivant « COOKIES CLUB » en Gill Sans — c'est POUR ÇA qu'on inline : la police de la page s'applique).
- Produces: `<BrandLogo size?: number>` (SVG inline, `role="img"`, `aria-label="Cookies Club"`) ; `<MapChrome />` (pastille logo haut-gauche + crédit « by Zucchini Studio » bas-gauche).

- [ ] **Step 1 : Générer `BrandLogo.tsx`**

```bash
S="$TMPDIR/cookie-svg"
npx svgo --config "$S/svgo.config.mjs" -i "public/brand/logo.svg" -o "$S/logo.svg"
wc -c "$S/logo.svg"
```

Créer `src/components/BrandLogo.tsx` : coller le `<svg>` optimisé converti en JSX (attributs camelCase — corriger via `npx tsc --noEmit`), avec la racine remplacée par :

```tsx
// Logo de marque inliné (public/brand/logo.svg optimisé svgo — ne pas éditer à la
// main). Inline OBLIGATOIREMENT : le lettrage « COOKIES CLUB » est du texte vivant
// en Gill Sans Ultra Bold, il n'hérite de la police que dans le DOM de la page.
export function BrandLogo({ size = 44 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 300 300" role="img" aria-label="Cookies Club">
      {/* … contenu optimisé … */}
    </svg>
  )
}
```

Si l'optimisé garde des `<text font-family="GillSans-UltraBold …">` : remplacer cet attribut par `fontFamily="var(--font-title)"` (c'est le point critique de la tâche — vérifier visuellement en QA que le lettrage est bien en Gill Sans).

- [ ] **Step 2 : Test qui échoue**

```tsx
// src/components/__tests__/map-chrome.test.tsx
import { expect, test } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MapChrome } from '../MapChrome'

test('pastille logo accessible + crédit Zucchini Studio', () => {
  render(<MapChrome />)
  expect(screen.getByRole('img', { name: 'Cookies Club' })).toBeDefined()
  expect(screen.getByText('by Zucchini Studio')).toBeDefined()
})
```

Run: Expected FAIL.

- [ ] **Step 3 : Implémenter `src/components/MapChrome.tsx`**

```tsx
import { BrandLogo } from './BrandLogo'

// Habillage variante A (validée sur maquette, mémoire projet) : logo haut-gauche,
// crédit bas-gauche. B (bandeau) et C (crédit sous logo) sont en réserve.
export function MapChrome() {
  return (
    <>
      <div className="absolute left-3 top-[calc(0.75rem+env(safe-area-inset-top))] z-10 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-1.5 shadow-[var(--shadow-chip)]">
        <BrandLogo size={44} />
      </div>
      <p className="absolute bottom-[calc(0.5rem+env(safe-area-inset-bottom))] left-3 z-10 rounded-full bg-[color:var(--surface)]/80 px-2.5 py-1 text-[10px] text-[color:var(--text-muted)]">
        by Zucchini Studio
      </p>
    </>
  )
}
```

Dans `CookieMap.tsx`, ajouter `<MapChrome />` dans le wrapper `relative h-dvh`, juste avant la puce de langue (rendu quel que soit `renderer` — webgl, raster ou erreur).

- [ ] **Step 4 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/components/BrandLogo.tsx src/components/MapChrome.tsx src/components/__tests__/map-chrome.test.tsx src/components/CookieMap.tsx
git commit -m "feat: habillage carte — logo Cookies Club et crédit Zucchini Studio (variante A)"
```

---

### Task 12 : Fiche — CTA avec icônes et largeurs stables

**Files:**
- Modify: `src/components/ShopSheet.tsx` (rangée de CTA, lignes 87-115), `src/lib/i18n.ts` (raccourcir `copyAddress`)
- Test: `src/components/__tests__/shop-sheet-cta.test.tsx`

**Interfaces:**
- Consumes: `IconDirections`, `IconCopy`, `IconShare`, `IconCheck` (Task 4).
- Produces: grille `grid grid-cols-3` — contrat de non-décalage : la largeur des boutons ne dépend plus du libellé.

- [ ] **Step 1 : i18n**

`copyAddress: 'Copier l’adresse'` → `'Copier'` (en : `'Copy'`) — le libellé court tient dans la colonne ; l'`aria-label` complet reste « Copier l'adresse » (clé existante inchangée en langage naturel : ajouter `copyAddressFull: 'Copier l’adresse'` / `'Copy address'` pour l'aria-label).

- [ ] **Step 2 : Test qui échoue**

```tsx
// src/components/__tests__/shop-sheet-cta.test.tsx
import { expect, test } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'
import { LangProvider } from '../LangProvider'
import { ShopSheet } from '../ShopSheet'

const shop = {
  id: 1, slug: 'test', name: 'Test', address: '1 rue Test',
  lat: 45.5, lng: -73.5, googleMapsUrl: 'https://maps.google.com/x',
  rating: 4, review: 'Bon.',
}

function renderSheet() {
  return render(
    <LangProvider>
      <ShopSheet shop={shop} onClose={() => {}} />
    </LangProvider>
  )
}

test('les 3 CTA vivent dans une grille à colonnes égales, avec icône', () => {
  const { container } = renderSheet()
  const grid = container.querySelector('.grid.grid-cols-3')
  expect(grid).not.toBeNull()
  expect(grid!.querySelectorAll('button')).toHaveLength(3)
  expect(grid!.querySelectorAll('svg')).toHaveLength(3)
})

test('copier garde sa colonne : le libellé change, pas la structure', async () => {
  Object.assign(navigator, { clipboard: { writeText: async () => {} } })
  renderSheet()
  const btn = screen.getByRole('button', { name: 'Copier l’adresse' })
  fireEvent.click(btn)
  expect(await screen.findByText('Copié')).toBeDefined()
  expect(screen.getByRole('button', { name: 'Copier l’adresse' })).toBe(btn) // aria stable
})
```

Run: Expected FAIL (pas de grille).

- [ ] **Step 3 : Implémenter**

Remplacer la rangée `flex flex-wrap` (lignes 88-115) par :

```tsx
<div className="grid grid-cols-3 gap-2">
  <button
    onClick={onDirections}
    className="inline-flex h-11 items-center justify-center gap-1.5 whitespace-nowrap rounded-full bg-[color:var(--btn-bg)] px-3 text-[13px] font-medium text-[color:var(--btn-text)] transition-colors hover:bg-[color:var(--btn-bg-hover)] active:scale-[0.98]"
  >
    <IconDirections size={15} />
    {t('directions')}
  </button>
  <button
    onClick={onCopy}
    aria-label={t('copyAddressFull')}
    className="inline-flex h-11 items-center justify-center gap-1.5 whitespace-nowrap rounded-full border border-[color:var(--border-strong)] px-3 text-[13px] text-[color:var(--text-body)] transition-colors hover:bg-[color:var(--surface-2)] active:scale-[0.98]"
  >
    {copied ? <IconCheck size={15} /> : <IconCopy size={15} />}
    {copied ? t('copied') : t('copyAddress')}
  </button>
  <button
    onClick={onShare}
    className="inline-flex h-11 items-center justify-center gap-1.5 whitespace-nowrap rounded-full border border-[color:var(--border-strong)] px-3 text-[13px] text-[color:var(--text-body)] transition-colors hover:bg-[color:var(--surface-2)] active:scale-[0.98]"
  >
    {linkCopied ? <IconCheck size={15} /> : <IconShare size={15} />}
    {linkCopied ? t('linkCopied') : t('share')}
  </button>
</div>
<div className="mt-2 text-right">
  <a href={shop.googleMapsUrl} target="_blank" rel="noopener noreferrer" className="py-2.5 text-[13px] text-[color:var(--text-muted)] underline-offset-4 transition-colors hover:text-[color:var(--accent-ink)] hover:underline">
    <span className="inline-flex items-center gap-1">{t('googleListing')} <IconExternal size={13} /></span>
  </a>
</div>
```

- [ ] **Step 4 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/components/ShopSheet.tsx src/lib/i18n.ts src/components/__tests__/shop-sheet-cta.test.tsx
git commit -m "fix: CTA de la fiche — icônes et grille à largeur stable (plus de décalage au clic)"
```

---

### Task 13 : Modal itinéraire

**Files:**
- Create: `src/components/DirectionsModal.tsx`
- Test: `src/components/__tests__/directions-modal.test.tsx`
- Modify: `src/lib/nav-links.ts` (+`wazeUrl`), `src/lib/__tests__/nav-links.test.ts` (créer si absent), `src/components/ShopSheet.tsx` (supprimer `platform()`, `iosChooser`, import `geoUri` ; `onDirections` ouvre la modal), `src/lib/i18n.ts` (clés)

**Interfaces:**
- Consumes: `googleDirectionsUrl`, `appleMapsUrl` (existants), `IconClose` (Task 4).
- Produces: `wazeUrl(lat: number, lng: number): string` ; `<DirectionsModal lat lng onClose>` — `role="dialog"`, ferme sur Échap/backdrop/croix.

- [ ] **Step 1 : `wazeUrl` en TDD**

Test (`src/lib/__tests__/nav-links.test.ts`) :

```ts
import { expect, test } from 'vitest'
import { appleMapsUrl, googleDirectionsUrl, wazeUrl } from '../nav-links'

test('les 3 liens d’itinéraire pointent la destination', () => {
  expect(wazeUrl(45.5, -73.6)).toBe('https://waze.com/ul?ll=45.5%2C-73.6&navigate=yes')
  expect(appleMapsUrl(45.5, -73.6)).toContain('45.5,-73.6')
  expect(googleDirectionsUrl(45.5, -73.6)).toContain(encodeURIComponent('45.5,-73.6'))
})
```

Run: FAIL. Implémentation dans `nav-links.ts` :

```ts
export function wazeUrl(lat: number, lng: number): string {
  return `https://waze.com/ul?ll=${encodeURIComponent(`${lat},${lng}`)}&navigate=yes`
}
```

Run: PASS.

- [ ] **Step 2 : i18n**

fr : `directionsTitle: 'S’y rendre avec…'`, `waze: 'Waze'` (les clés `openInPlans`/`openInGoogleMaps` existent ; harmoniser leurs libellés en `'Apple Plans'`/`'Google Maps'` court dans la modal : ajouter `plansShort: 'Apple Plans'`, `googleMapsShort: 'Google Maps'` identiques en/fr).
en : `directionsTitle: 'Get there with…'`.

- [ ] **Step 3 : Test de la modal (échoue)**

```tsx
// src/components/__tests__/directions-modal.test.tsx
import { expect, test, vi } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'
import { LangProvider } from '../LangProvider'
import { DirectionsModal } from '../DirectionsModal'

function renderModal(onClose = vi.fn()) {
  render(
    <LangProvider>
      <DirectionsModal lat={45.5} lng={-73.6} onClose={onClose} />
    </LangProvider>
  )
  return onClose
}

test('3 liens externes : Google Maps, Apple Plans, Waze', () => {
  renderModal()
  const links = screen.getAllByRole('link')
  expect(links).toHaveLength(3)
  for (const l of links) {
    expect(l.getAttribute('target')).toBe('_blank')
    expect(l.getAttribute('rel')).toContain('noopener')
  }
  expect(links.map((l) => l.getAttribute('href') ?? '').join(' ')).toMatch(/google.*apple.*waze|waze/s)
})

test('Échap et backdrop ferment', () => {
  const onClose = renderModal()
  fireEvent.keyDown(window, { key: 'Escape' })
  expect(onClose).toHaveBeenCalledTimes(1)
  fireEvent.click(screen.getByRole('dialog'))
  expect(onClose).toHaveBeenCalledTimes(2)
})
```

Run: Expected FAIL.

- [ ] **Step 4 : Implémenter `DirectionsModal.tsx`**

```tsx
'use client'

import { useEffect, useRef } from 'react'
import { appleMapsUrl, googleDirectionsUrl, wazeUrl } from '@/lib/nav-links'
import { useLang } from './LangProvider'
import { IconClose } from './icons'

export function DirectionsModal({ lat, lng, onClose }: { lat: number; lng: number; onClose: () => void }) {
  const { t } = useLang()
  const firstRef = useRef<HTMLAnchorElement>(null)
  useEffect(() => {
    firstRef.current?.focus()
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose()
    }
    window.addEventListener('keydown', onKey)
    return () => window.removeEventListener('keydown', onKey)
  }, [onClose])

  const rows = [
    { href: googleDirectionsUrl(lat, lng), label: t('googleMapsShort') },
    { href: appleMapsUrl(lat, lng), label: t('plansShort') },
    { href: wazeUrl(lat, lng), label: t('waze') },
  ]

  return (
    <div
      role="dialog"
      aria-modal="true"
      aria-label={t('directionsTitle')}
      onClick={onClose}
      className="fixed inset-0 z-30 flex items-end justify-center bg-black/40 sm:items-center"
    >
      <div
        onClick={(e) => e.stopPropagation()}
        className="w-full rounded-t-[var(--radius-sheet)] border-t border-[color:var(--border)] bg-[color:var(--surface)] p-5 pb-[calc(1.25rem+env(safe-area-inset-bottom))] sm:w-[340px] sm:rounded-[var(--radius-card)] sm:border"
      >
        <div className="mb-3 flex items-center justify-between">
          <h3 className="font-display text-[16px] text-[color:var(--text-strong)]">{t('directionsTitle')}</h3>
          <button aria-label={t('close')} onClick={onClose} className="flex h-8 w-8 items-center justify-center rounded-full text-[color:var(--text-muted)] hover:bg-[color:var(--surface-2)]">
            <IconClose />
          </button>
        </div>
        <div className="flex flex-col gap-1.5">
          {rows.map((r, i) => (
            <a
              key={r.label}
              ref={i === 0 ? firstRef : undefined}
              href={r.href}
              target="_blank"
              rel="noopener noreferrer"
              className="flex h-12 items-center rounded-[var(--radius-field)] border border-[color:var(--border)] px-4 text-[14px] text-[color:var(--text-body)] transition-colors hover:bg-[color:var(--surface-2)]"
            >
              {r.label}
            </a>
          ))}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 5 : Brancher dans `ShopSheet.tsx`**

- Supprimer `platform()` (lignes 9-14), l'état `iosChooser`, le bloc lignes 117-132, l'import `geoUri`/`appleMapsUrl` devenus inutiles ici.
- `const [directionsOpen, setDirectionsOpen] = useState(false)` ; `onDirections = () => setDirectionsOpen(true)`.
- En fin de JSX : `{directionsOpen && <DirectionsModal lat={shop.lat} lng={shop.lng} onClose={() => setDirectionsOpen(false)} />}`.

- [ ] **Step 6 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/components/DirectionsModal.tsx src/components/__tests__/directions-modal.test.tsx src/components/ShopSheet.tsx src/lib/nav-links.ts src/lib/__tests__/nav-links.test.ts src/lib/i18n.ts
git commit -m "feat: itinéraire en modal — Google Maps, Apple Plans, Waze"
```

---

### Task 14 : Fiche — bottom-sheet à deux crans (geste vertical)

**Files:**
- Modify: `src/components/ShopSheet.tsx` (zone de poignée + états), `src/app/globals.css` (le handle ::before existant reste)
- Test: `src/components/__tests__/shop-sheet-drag.test.tsx`

**Interfaces:**
- Produces: comportement — glisser vers le haut sur la poignée/l'en-tête agrandit (`data-expanded="true"`, `h-[92dvh]`), vers le bas réduit puis ferme (`onClose`). La zone scrollable interne garde son défilement natif.

- [ ] **Step 1 : Test qui échoue**

```tsx
// src/components/__tests__/shop-sheet-drag.test.tsx
import { expect, test, vi } from 'vitest'
import { fireEvent, render } from '@testing-library/react'
import { LangProvider } from '../LangProvider'
import { ShopSheet } from '../ShopSheet'

const shop = {
  id: 1, slug: 'test', name: 'Test', address: '1 rue Test',
  lat: 45.5, lng: -73.5, googleMapsUrl: 'https://maps.google.com/x',
  rating: 4, review: 'Bon.',
}

function grab(container: HTMLElement) {
  const zone = container.querySelector('[data-drag-zone]')!
  return zone
}

test('glisser vers le haut agrandit la fiche', () => {
  const { container } = render(
    <LangProvider><ShopSheet shop={shop} onClose={() => {}} /></LangProvider>
  )
  const sheet = container.querySelector('.cmtl-sheet')!
  expect(sheet.getAttribute('data-expanded')).toBe('false')
  const zone = grab(container as HTMLElement)
  fireEvent.pointerDown(zone, { clientY: 500, pointerId: 1 })
  fireEvent.pointerMove(zone, { clientY: 400, pointerId: 1 })
  fireEvent.pointerUp(zone, { clientY: 400, pointerId: 1 })
  expect(sheet.getAttribute('data-expanded')).toBe('true')
})

test('glisser vers le bas depuis le cran compact ferme', () => {
  const onClose = vi.fn()
  const { container } = render(
    <LangProvider><ShopSheet shop={shop} onClose={onClose} /></LangProvider>
  )
  const zone = grab(container as HTMLElement)
  fireEvent.pointerDown(zone, { clientY: 400, pointerId: 1 })
  fireEvent.pointerUp(zone, { clientY: 500, pointerId: 1 })
  expect(onClose).toHaveBeenCalled()
})

test('glisser vers le bas depuis le cran étendu réduit sans fermer', () => {
  const onClose = vi.fn()
  const { container } = render(
    <LangProvider><ShopSheet shop={shop} onClose={onClose} /></LangProvider>
  )
  const sheet = container.querySelector('.cmtl-sheet')!
  const zone = grab(container as HTMLElement)
  fireEvent.pointerDown(zone, { clientY: 500, pointerId: 1 })
  fireEvent.pointerUp(zone, { clientY: 380, pointerId: 1 }) // agrandit
  fireEvent.pointerDown(zone, { clientY: 300, pointerId: 1 })
  fireEvent.pointerUp(zone, { clientY: 420, pointerId: 1 }) // réduit
  expect(sheet.getAttribute('data-expanded')).toBe('false')
  expect(onClose).not.toHaveBeenCalled()
})
```

Run: Expected FAIL (pas de `data-drag-zone`).

- [ ] **Step 2 : Implémenter dans `ShopSheet.tsx`**

États et handlers :

```tsx
const [expanded, setExpanded] = useState(false)
const [dragY, setDragY] = useState(0)
const dragRef = useRef<{ startY: number; startT: number } | null>(null)

const onDragStart = (e: React.PointerEvent) => {
  dragRef.current = { startY: e.clientY, startT: e.timeStamp }
  ;(e.currentTarget as HTMLElement).setPointerCapture?.(e.pointerId)
}
const onDragMove = (e: React.PointerEvent) => {
  if (!dragRef.current) return
  setDragY(e.clientY - dragRef.current.startY)
}
const onDragEnd = (e: React.PointerEvent) => {
  if (!dragRef.current) return
  const dy = e.clientY - dragRef.current.startY
  const vy = dy / Math.max(1, e.timeStamp - dragRef.current.startT) // px/ms
  dragRef.current = null
  setDragY(0)
  if (dy < -50 || vy < -0.4) setExpanded(true)
  else if (dy > 50 || vy > 0.4) {
    if (expanded) setExpanded(false)
    else onClose()
  }
}
```

Sur le conteneur racine (ligne 58) :

- `max-h-[70dvh]` → `` className={`… ${expanded ? 'h-[92dvh] max-h-[92dvh]' : 'max-h-[70dvh]'} transition-[max-height,height] duration-200 …`} `` (garder toutes les classes existantes) ;
- ajouter `data-expanded={expanded ? 'true' : 'false'}` ;
- ajouter `style={{ transform: dragY > 0 ? `translateY(${dragY}px)` : undefined, transition: dragY ? 'none' : undefined }}` (la fiche suit le doigt vers le bas — indice visuel de fermeture ; vers le haut, l'agrandissement se joue au relâcher).

Zone de poignée, premier enfant du conteneur (au-dessus du bouton fermer) :

```tsx
{/* Poignée de drag : couvre la bande du handle + le haut de l'en-tête. touch-none
    pour que le navigateur n'interprète pas le geste comme un scroll. Cachée en
    desktop (sm:) où la fiche est une carte flottante sans crans. */}
<div
  data-drag-zone
  onPointerDown={onDragStart}
  onPointerMove={onDragMove}
  onPointerUp={onDragEnd}
  onPointerCancel={onDragEnd}
  className="absolute inset-x-0 top-0 z-10 h-10 cursor-grab touch-none sm:hidden"
/>
```

(Le bouton fermer, `right-3 top-4`, doit rester cliquable : lui ajouter `z-20`.)

- [ ] **Step 3 : Vérifier et committer**

Run: `npm test` — Expected: PASS (dont `shop-sheet-ssr.test.tsx` : le rendu SSR ne doit pas casser — les handlers ne s'exécutent pas côté serveur).

```bash
git add src/components/ShopSheet.tsx src/components/__tests__/shop-sheet-drag.test.tsx
git commit -m "feat: fiche à deux crans — geste vertical pour agrandir, réduire, fermer"
```

---

### Task 15 : SEO — metadata bilingues + canonical

**Files:**
- Modify: `src/app/layout.tsx` (metadata complet), `src/app/c/[slug]/page.tsx` (generateMetadata)
- Test: `src/app/c/[slug]/__tests__/metadata.test.ts`

**Interfaces:**
- Consumes: `SITE_*`, `shopTitle`, `shopDescription` (Task 2).
- Produces: metadata finaux ; contrat testé sur `generateMetadata`.

- [ ] **Step 1 : Test qui échoue**

```ts
// src/app/c/[slug]/__tests__/metadata.test.ts
import { expect, test, vi } from 'vitest'

vi.mock('@/lib/shops', () => ({
  getShopBySlug: vi.fn(async (slug: string) =>
    slug === 'existe'
      ? { id: 1, slug: 'existe', name: 'Félix', address: '1 rue Rachel', lat: 45.5, lng: -73.6, googleMapsUrl: 'x', rating: 4.5, review: 'Top cookie.' }
      : null
  ),
  listShops: vi.fn(async () => []),
}))

test('generateMetadata : title, description bilingue, canonical', async () => {
  const { generateMetadata } = await import('../page')
  const md = await generateMetadata({ params: Promise.resolve({ slug: 'existe' }) } as never)
  expect(md.title).toBe('Félix — Cookies Club Montréal')
  expect(md.description).toContain('4,5')
  expect(md.description).toContain('cookie')
  expect(md.alternates?.canonical).toBe('/c/existe')
})

test('fiche inconnue : fallback au nom du site', async () => {
  const { generateMetadata } = await import('../page')
  const md = await generateMetadata({ params: Promise.resolve({ slug: 'absente' }) } as never)
  expect(md.title).toBe('Cookies Club — Montréal')
})
```

Run: Expected FAIL (canonical absent aujourd'hui).

- [ ] **Step 2 : Implémenter**

`c/[slug]/page.tsx` :

```tsx
export async function generateMetadata({ params }: PageProps<'/c/[slug]'>): Promise<Metadata> {
  const { slug } = await params
  const shop = await getShopBySlug(slug)
  if (!shop) return { title: SITE_NAME }
  const title = shopTitle(shop.name)
  const description = shopDescription(shop)
  return {
    title,
    description,
    alternates: { canonical: `/c/${slug}` },
    openGraph: { title, description, siteName: SITE_NAME, type: 'article' },
  }
}
```

`layout.tsx` — metadata final :

```tsx
export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: SITE_TITLE,
  description: SITE_DESCRIPTION,
  alternates: { canonical: '/' },
  keywords: ['cookie map', 'cookies montréal', 'cookie montreal', 'cookies mtl', 'meilleurs cookies montréal', 'best cookies montreal'],
  openGraph: {
    siteName: SITE_NAME,
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
    type: 'website',
    locale: 'fr_CA',
    alternateLocale: 'en_CA',
  },
};
```

- [ ] **Step 3 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/app/layout.tsx "src/app/c/[slug]/page.tsx" "src/app/c/[slug]/__tests__/metadata.test.ts"
git commit -m "feat: metadata SEO bilingues — titles, descriptions, canonical, OG"
```

---

### Task 16 : Sitemap + robots

**Files:**
- Create: `src/app/sitemap.ts`
- Test: `src/app/__tests__/sitemap.test.ts`
- Modify: `src/app/robots.ts`

**Interfaces:**
- Consumes: `listShops` (existant), `SITE_URL` (Task 2).
- Produces: `/sitemap.xml` (accueil + chaque `/c/[slug]`), référencé par robots.

- [ ] **Step 1 : Test qui échoue**

```ts
// src/app/__tests__/sitemap.test.ts
import { expect, test, vi } from 'vitest'

vi.mock('@/lib/shops', () => ({
  listShops: vi.fn(async () => [
    { id: 1, slug: 'felix', name: 'Félix', address: 'a', lat: 0, lng: 0, googleMapsUrl: 'x', rating: 4, review: 'r' },
    { id: 2, slug: 'norton', name: 'Norton', address: 'b', lat: 0, lng: 0, googleMapsUrl: 'x', rating: 5, review: 'r' },
  ]),
}))

test('sitemap : accueil + une entrée par fiche, sur le domaine de prod', async () => {
  const sitemap = (await import('../sitemap')).default
  const entries = await sitemap()
  expect(entries[0].url).toBe('https://cookies.zucchinistudio.com')
  expect(entries.map((e) => e.url)).toContain('https://cookies.zucchinistudio.com/c/felix')
  expect(entries).toHaveLength(3)
})
```

Run: Expected FAIL (module inexistant).

- [ ] **Step 2 : Implémenter `src/app/sitemap.ts`**

```ts
import type { MetadataRoute } from 'next'
import { listShops } from '@/lib/shops'
import { SITE_URL } from '@/lib/site'

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const shops = await listShops()
  return [
    { url: SITE_URL, changeFrequency: 'weekly', priority: 1 },
    ...shops.map((s) => ({
      url: `${SITE_URL}/c/${s.slug}`,
      changeFrequency: 'monthly' as const,
      priority: 0.8,
    })),
  ]
}
```

`robots.ts` :

```ts
import type { MetadataRoute } from 'next'
import { SITE_URL } from '@/lib/site'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: '*', allow: '/', disallow: ['/admin', '/api/'] },
    sitemap: `${SITE_URL}/sitemap.xml`,
  }
}
```

- [ ] **Step 3 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/app/sitemap.ts src/app/__tests__/sitemap.test.ts src/app/robots.ts
git commit -m "feat: sitemap dynamique et robots à jour"
```

---

### Task 17 : JSON-LD (Bakery + Review, ItemList)

**Files:**
- Create: `src/lib/jsonld.ts`
- Test: `src/lib/__tests__/jsonld.test.ts`
- Modify: `src/app/page.tsx` (ItemList), `src/app/c/[slug]/page.tsx` (Bakery)

**Interfaces:**
- Consumes: `Shop` (existant), `SITE_NAME`, `SITE_URL` (Task 2).
- Produces: `shopJsonLd(shop: Shop): object`, `homeJsonLd(shops: Shop[]): object`.

- [ ] **Step 1 : Test qui échoue**

```ts
// src/lib/__tests__/jsonld.test.ts
import { expect, test } from 'vitest'
import { homeJsonLd, shopJsonLd } from '../jsonld'

const shop = {
  id: 1, slug: 'felix', name: 'Félix', address: '1 rue Rachel E',
  lat: 45.52, lng: -73.58, googleMapsUrl: 'x', rating: 4.5, review: 'Croustillant.',
}

test('Bakery avec Review signé Cookies Club, SANS aggregateRating', () => {
  const ld = shopJsonLd(shop) as Record<string, unknown>
  expect(ld['@type']).toBe('Bakery')
  expect(ld.name).toBe('Félix')
  expect(JSON.stringify(ld)).not.toContain('aggregateRating')
  const review = ld.review as Record<string, unknown>
  expect((review.author as Record<string, unknown>).name).toBe('Cookies Club — Montréal')
  expect((review.reviewRating as Record<string, unknown>).ratingValue).toBe(4.5)
})

test('ItemList ordonné des fiches', () => {
  const ld = homeJsonLd([shop]) as Record<string, unknown>
  expect(ld['@type']).toBe('ItemList')
  const items = ld.itemListElement as Array<Record<string, unknown>>
  expect(items[0].url).toBe('https://cookies.zucchinistudio.com/c/felix')
})
```

Run: Expected FAIL.

- [ ] **Step 2 : Implémenter `src/lib/jsonld.ts`**

```ts
import type { Shop } from './shops'
import { SITE_NAME, SITE_URL } from './site'

// Pas d'aggregateRating : la note est un avis éditorial unique (celui de Cookies
// Club), pas une moyenne d'avis — un aggregateRating serait trompeur pour Google.
export function shopJsonLd(shop: Shop): object {
  return {
    '@context': 'https://schema.org',
    '@type': 'Bakery',
    name: shop.name,
    url: `${SITE_URL}/c/${shop.slug}`,
    address: {
      '@type': 'PostalAddress',
      streetAddress: shop.address,
      addressLocality: 'Montréal',
      addressRegion: 'QC',
      addressCountry: 'CA',
    },
    geo: { '@type': 'GeoCoordinates', latitude: shop.lat, longitude: shop.lng },
    review: {
      '@type': 'Review',
      author: { '@type': 'Organization', name: SITE_NAME, url: SITE_URL },
      reviewRating: { '@type': 'Rating', ratingValue: shop.rating, bestRating: 5, worstRating: 0 },
      reviewBody: shop.review,
    },
  }
}

export function homeJsonLd(shops: Shop[]): object {
  return {
    '@context': 'https://schema.org',
    '@type': 'ItemList',
    name: SITE_NAME,
    url: SITE_URL,
    itemListElement: shops.map((s, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: s.name,
      url: `${SITE_URL}/c/${s.slug}`,
    })),
  }
}
```

- [ ] **Step 3 : Injecter dans les pages**

`page.tsx` (`MapWithShops`) et `c/[slug]/page.tsx` (`MapForShop`) — après récupération des données :

```tsx
<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(homeJsonLd(shops)) }} />
```

(respectivement `shopJsonLd(shop)`), rendu à côté du `<CookieMap …>` dans un fragment.

- [ ] **Step 4 : Vérifier et committer**

Run: `npm test` — Expected: PASS.

```bash
git add src/lib/jsonld.ts src/lib/__tests__/jsonld.test.ts src/app/page.tsx "src/app/c/[slug]/page.tsx"
git commit -m "feat: données structurées JSON-LD — Bakery+Review par fiche, ItemList à l'accueil"
```

---

### Task 18 : Images OG aux couleurs de la marque (Gill Sans + cookies)

**Files:**
- Create: `src/app/opengraph-image.tsx` (accueil)
- Modify: `src/app/c/[slug]/opengraph-image.tsx` (refonte)

**Interfaces:**
- Consumes: `src/fonts/gill-sans-ultra-bold.otf` (chargé via `node:fs`), `SITE_BRAND`, `SITE_TITLE` (Task 2), palette du spec (`#f3ede3`, `#502712`, `#a4794a`, `#f3c787`).
- Produces: OG 1200×630 accueil + fiche, lettrage en vraie Gill Sans.

Note satori : pas de `<use>`/sprite ici — satori rasterise un arbre JSX autonome. Les cookies de note sont dessinés en pastilles (cercles pleins/vides aux couleurs cookie) — robuste et sans dépendance au texte SVG de la bannière. Si Léo veut la bannière exacte plus tard : la rasteriser en PNG (sharp est déjà en devDependencies du pipeline tuiles) et la poser en `backgroundImage`.

- [ ] **Step 1 : OG accueil `src/app/opengraph-image.tsx`**

```tsx
import { readFile } from 'node:fs/promises'
import { join } from 'node:path'
import { ImageResponse } from 'next/og'
import { SITE_BRAND } from '@/lib/site'

export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'
export const alt = 'Cookies Club — Montréal'

export default async function Image() {
  const gill = await readFile(join(process.cwd(), 'src/fonts/gill-sans-ultra-bold.otf'))
  return new ImageResponse(
    (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          background: '#f3ede3',
          fontFamily: 'Gill Sans Ultra',
        }}
      >
        <div style={{ fontSize: 110, color: '#502712' }}>{SITE_BRAND}</div>
        <div style={{ fontSize: 44, color: '#a4794a', marginTop: 10 }}>Montréal</div>
        <div style={{ display: 'flex', gap: 18, marginTop: 42 }}>
          {[0, 1, 2, 3, 4].map((i) => (
            <div key={i} style={{ width: 56, height: 56, borderRadius: 28, background: '#d0914a', border: '6px solid #502712', display: 'flex' }} />
          ))}
        </div>
        <div style={{ fontSize: 28, color: '#7d6d5b', marginTop: 40 }}>La carte des cookies · Montreal&apos;s cookie map</div>
      </div>
    ),
    { ...size, fonts: [{ name: 'Gill Sans Ultra', data: gill, weight: 700, style: 'normal' }] }
  )
}
```

- [ ] **Step 2 : Refondre l'OG fiche**

Même squelette (fond `#f3ede3`, `fontFamily: 'Gill Sans Ultra'`, police chargée pareil) :
- ligne 1 : `SITE_BRAND` en 40 px `#a4794a` + « Montréal » ;
- nom du commerce en 84 px `#502712` (centré, padding 60) ;
- rangée de 5 pastilles 44 px : pleines (`background: '#d0914a'`) pour `Math.floor(rating)`, demi (dégradé `linear-gradient(90deg, #d0914a 50%, #f3ede3 50%)`) si `rating % 1 >= 0.5`, vides (`background: '#f3ede3'`) sinon — toutes avec `border: '5px solid #502712'` ;
- la note en chiffres « 4,5 / 5 » en 36 px `#7d6d5b`.

Supprimer le `🍪` existant (règle sans-emoji, elle vaut aussi pour les images).

- [ ] **Step 3 : Vérifier visuellement**

```bash
npm run dev &
sleep 8
curl -s -o "$TMPDIR/og-home.png" -w "%{http_code}\n" http://localhost:3000/opengraph-image
# récupérer un slug réel depuis le sitemap, puis tester l'OG de cette fiche
SLUG=$(curl -s http://localhost:3000/sitemap.xml | grep -o '/c/[^<]*' | head -1)
curl -s -o "$TMPDIR/og-shop.png" -w "%{http_code}\n" "http://localhost:3000${SLUG}/opengraph-image"
```

Expected: 200 + 200 ; ouvrir les deux PNG (outil Read) et vérifier : lettrage Gill Sans, pastilles de note, aucune police de secours. Arrêter le dev server.

- [ ] **Step 4 : Commit**

```bash
git add src/app/opengraph-image.tsx "src/app/c/[slug]/opengraph-image.tsx"
git commit -m "feat: images OG de partage aux couleurs Cookies Club (Gill Sans self-host)"
```

---

### Task 19 : Passe finale — lint, tests, build, PR

**Files:**
- Modify: ce qui ressort du lint/build uniquement.

- [ ] **Step 1 : Suite complète**

Run: `npm test` — Expected: PASS, zéro skip.
Run: `npx eslint src` — Expected: zéro erreur.
Run: `npm run build` — Expected: succès (vérifie next/font, sitemap, OG en conditions réelles).

- [ ] **Step 2 : Balayage des invariants**

- `grep -rn "Cookies MTL" src/ public/manifest*` → uniquement des commentaires historiques.
- `grep -rnP "[🍪✕◎↗✓]|prefers-color-scheme" src/ --include=*.tsx --include=*.ts` → plus d'emoji/caractère décoratif dans le code UI ; `prefers-color-scheme` ne subsiste que dans `theme.ts` (résolution système) et `map-style.ts` (fallback).
- `git status` → aucun fichier orphelin non committé (le bloc AGENTS.md re-généré par `next dev` peut apparaître : le committer tel quel).

- [ ] **Step 3 : Pousser et ouvrir la PR (sans merger)**

```bash
git push -u origin feature/v1.1-visuel-ux
gh pr create --title "v1.1 — identité Cookies Club, thème manuel, UX fiche, SEO" --body "$(cat <<'EOF'
## Quoi
- Identité : Gill Sans Ultra Bold + Comfortaa, logo + crédit Zucchini Studio (habillage A), renommage « Cookies Club — Montréal », notes et marqueurs aux SVG de la marque
- Thème : toggle manuel clair/sombre (localStorage > système), les deux cartes suivent à chaud
- Fiche : bottom-sheet à deux crans (geste vertical), CTA avec icônes et largeurs stables, itinéraire en modal (Google Maps / Apple Plans / Waze)
- Admin : saisie de note au slider avec visualisation cookies, lien « Voir la carte »
- SEO : metadata bilingues + canonical, sitemap, JSON-LD (Bakery+Review, ItemList), images OG en vraie Gill Sans

## Spec & plan
- docs/superpowers/specs/2026-08-08-ajustements-visuels-v1.1-design.md
- docs/superpowers/plans/2026-08-08-v1-1-visuel-ux.md

## QA
- `npm test` vert, `npm run build` vert
- QA locale par Léo AVANT merge (rounds de retours prévus)
EOF
)"
```

Ne PAS merger : signaler la PR à Léo pour sa QA locale.
